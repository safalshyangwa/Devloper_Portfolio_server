info:
  name: post
  type: http
  seq: 2

http:
  method: POST
  url: http://localhost:8000/api/auth/signup
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
